import SessionsDashboard from "@/components/SessionsDashboard";

export const metadata = {
  title: "Sessions — Devin Admin",
};

export default function DashboardPage() {
  return <SessionsDashboard />;
}
